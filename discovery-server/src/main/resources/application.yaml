spring:
  application:
    name: discovery-server
security:
  user:
    name: root
    password: root
server:
  port: 8000
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://root:root@localhost:8000/eureka/